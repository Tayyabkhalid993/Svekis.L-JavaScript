

// Practice exercise 4.3

// 1. Create a Boolean value for an ID varaible.

const id = true;

/* 2. Using a ternary oprator, create a message variable that will check 
whether their ID is valid and either allow a person into a venue or not.
*/

const message = (id) ? "Allowed to In" : "Not Allowed to In";

// 3. Output to the console.

console.log(message);


